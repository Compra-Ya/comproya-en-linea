{
  "name": "comproya-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Backend de ComproYa en Línea (monolito modular, NestJS por construir). Por ahora solo trae Prisma y el script de siembra del catálogo — ver backend/README.md.",
  "type": "commonjs",
  "scripts": {
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev --name init",
    "seed": "ts-node prisma/seed.ts",
    "seed:test": "ts-node prisma/seed.ts --dry-run"
  },
  "dependencies": {
    "@prisma/client": "^5.20.0"
  },
  "devDependencies": {
    "@faker-js/faker": "^9.0.3",
    "@types/node": "^22.7.4",
    "prisma": "^5.20.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.2"
  },
  "prisma": {
    "seed": "ts-node prisma/seed.ts"
  }
}
